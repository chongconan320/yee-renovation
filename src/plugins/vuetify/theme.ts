import { type ThemeDefinition } from 'vuetify'

const defaultTheme: ThemeDefinition = {
  dark: false,
  colors: {
    primary: '#EF3340',
    dark: '#1c1b1b',
    white: '#FFF',
    grey: '#BEBCBC'
  }
}

export { defaultTheme }
